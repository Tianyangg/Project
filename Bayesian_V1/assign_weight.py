from Bayesian_V1 import define_CNF as df_CNF
from Bayesian_V1 import define_variables as df_v

# input is a part st = (i, namer)
# e.g: st = (A)
# return the corresponding weight
def assign_weight(st):
    # weights of theta
    print ("test")



assign_weight((1,2))